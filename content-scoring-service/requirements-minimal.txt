# Minimal requirements for fast development and container builds
# Core FastAPI and web server dependencies only
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Basic utilities
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Monitoring
prometheus-client>=0.19.0

# Basic NLP (lightweight alternatives)
# Use transformers instead of sentence-transformers for flexibility
transformers>=4.20.0
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# Basic text processing
nltk>=3.8
numpy>=1.24.3

# Optional: Add these only if ML features are needed
# sentence-transformers>=2.2.2
# scikit-learn>=1.3.2
# spacy>=3.7.2